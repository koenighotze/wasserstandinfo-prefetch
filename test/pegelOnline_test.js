const Lab = require('lab')
const lab = exports.lab = Lab.script()
const test = lab.test

lab.experiment('fetching the stations', () => {
  test('should fail on timeout', (done) => {
    done()
  })

  test('should return the payload as json', (done) => {
    done()
  })

  test('should fail on errors', (done) => {
    done()
  })
})