{
  "name": "wasserstandinfo-prefetch",
  "version": "1.0.0",
  "description": "todo",
  "main": "index.js",
  "author": "David Schmitz",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/koenighotze/wasserstandinfo-prefetch/issues"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:koenighotze/wasserstandinfo-prefetch.git"
  },
  "files": [
    "src/*"
  ],
  "scripts": {
    "clean": "/bin/rm -fr build/tmp/* build/*zip",
    "lint": "./node_modules/eslint/bin/eslint.js --fix src/** test/**",
    "test": "lab -r console -o stdout -r html -o build/report/coverage.html -r lcov -o build/report/lcov.info",
    "package": "pushd build/tmp && cp -rf ../../package.json ../../src/* . && npm i --production && zip -r -1 ../skill.zip * && popd",
    "update": "aws lambda update-function-code --function-name wasserstandinfoPrefetch --region eu-west-1 --zip-file fileb://build/skill.zip",
    "publish": "aws lambda update-function-code --function-name wasserstandinfoPrefetch --region eu-west-1 --zip-file fileb://build/skill.zip --publish",
    "invoke": "aws lambda invoke --function-name wasserstandinfoPrefetch --region eu-west-1 out.json",
    "logs": "awslogs get /aws/lambda/wasserstandinfoPrefetch --aws-region eu-west-1",
    "create.bucket": "aws s3 mb s3://dschmitz.wasserstandinfo",
    "delete.bucket": "aws s3 rb s3://dschmitz.wasserstandinfo"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.15",
    "aws-sdk": "^2.177.0",
    "wreck": "^12.2.0"
  },
  "devDependencies": {
    "aws-sdk-mock": "^1.7.0",
    "chai": "^4.1.2",
    "claudia": "^2.14.2",
    "eslint": "^4.16.0",
    "lab": "^13.1.0",
    "proxyquire": "^1.8.0",
    "request": "^2.81.0",
    "sinon": "^2.4.1"
  }
}
